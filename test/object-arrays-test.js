var chai = require('chai');
var expect = chai.expect;
var objectArrays = require('../object-arrays');

describe('sample', function () {
  it('should freaking work', function () {
    expect(object-arrays()).to.equal('booya');
  })
})
